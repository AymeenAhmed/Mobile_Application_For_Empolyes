<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.competition">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".IdentificationActivity"></activity>
        <activity android:name=".CardsActivity" />
        <activity android:name=".ReclamationActivity" />
        <activity android:name=".FreeActivity" />
        <activity android:name=".LeaveActivity" />
        <activity android:name=".JobActivity" />
        <activity android:name=".WorkActivity" />
        <activity android:name=".SalaryActivity" />
        <activity android:name=".PresenceActivity" />
        <activity android:name=".EmployeActivity2" />
        <activity android:name=".EmployeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>